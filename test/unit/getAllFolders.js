// @flow strict

const { describe, it, beforeEach } = require("mocha")
const { expect } = require("chai")

describe("unit/getAllFolders.js", () => {
	describe("calling with empty array", () => {
		it("should return empty array")
	})
})
